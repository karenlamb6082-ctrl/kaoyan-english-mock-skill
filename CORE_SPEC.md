# Core specification 1.1

## 目录

1. 真实性与优先级
2. 整卷生态和来源
3. 固定分类与各板块规则
4. 发布记录与校准
5. 格式规范

## 1. 真实性与优先级

整卷面向考生时必须像试卷，而不是练习拼盘。可能替代部分真题训练时，先写出支持和反对替代使用的最强论证，以及会改变判断的数据。约束冲突时按：来源事实、答案唯一、语言自然、构念有效、难度曲线、题型轮换、排版紧凑。允许记录有依据的例外，不得为了配额损坏题目。

完整结构：完形1—20、阅读A 21—40、阅读B 41—45、翻译46—50、写作A 51、写作B 52。

## 2. 整卷生态和来源

先记录每部分的主领域、具体事件、参与者、目标、机制、成本、作者解决方向、声音、开头方式、段落功能和结尾。主题名词、冲突机制、论证模板中有两层与近期卷相似时，默认更换或记录保留理由。四篇阅读通常来自四个领域，至少三个；最多两篇采用相同的折中结尾。

每部分允许多个 `source_ids`，但指定一个声音锚点。来源卡记录标题、作者、出版物、日期、访问日期、URL/DOI、核验状态、保留事实、删减和改写边界。每段或关键主张标记：`SOURCE_FACT`、`SOURCE_PARAPHRASE`、`SUPPORTED_SYNTHESIS`、`INVENTED_BRIDGE`、`AUTHORIAL_INTERPRETATION`。URL可访问不等于内容已核验；不能先写泛文再补一个看似合理的网址。

## 3. 固定分类与板块规则

### 完形

240—300词，20个可见空格，四列无框选项。主构念只能是 `lexical`、`collocation_preposition`、`logical_link`、`syntax_grammar`、`discourse_cohesion`。逻辑题说明两个命题的关系；语法题指出约束；搭配题指出结构；衔接题给出句外链条，否则重分为词义或重写。四项全部代入句子和段落，不接受无语法答案或两个自然答案。构念比例只作诊断，避免全部变成近义词题。

### 阅读A

每篇通常380—430词，总量约1520—1720词。主类型只用 `detail`、`inference`、`function`、`meaning`、`main_idea`、`attitude`，具体操作另写 `secondary_operation`。健康分布约为细节5—6、推断5—6、功能3—4、语义2—3、主旨/态度2—3，但不得硬凑。

最难篇至少三题实际需要跨段综合、应用或论证边界比较；全卷至少六题整合两个句子或完整段落，至少四个正确项不是显眼原句同义压缩。四个选项保持同一语义场、相近长度和语气。每篇至少一个强干扰项来自文章真实信息但错配对象、原因、阶段、功能或范围。不得依赖 always、only、all、every、never、completely、impossible、automatically 等绝对词批量制造错误项，也不能让正确项总是唯一谨慎项。

### 阅读B

采用当前真题基线支持的形式，按最近最少使用轮换，不永久封禁。排序/插入每个位置至少依靠两类线索：指代、词汇链、转折因果、时间顺序、语义推进。标题/匹配题检查词形和语义族的直接重合；答案应体现段落功能或边界，额外选项也应局部合理。约430—550词。

### 翻译

一个350—450词完整来源段落，五个25—35词左右的画线部分分散出现。难度来自原文支持的从句、非谓语、被动、抽象主语、插入和指代，需要自然汉语重组；不靠装饰性插入语。逐句核对指代、事实范围、画线文本和译文。

### 写作

Part A 明确身份、对象、目的、要点，必要时增加真实限制。Part B 必须有清晰黑白视觉，在图画和数据家庭间做覆盖轮换，不宣称官方机械交替。双图必须提供互补视角，显示样本、单位、时期、类别和单/多选属性；第二图应改变第一图的解释而非重复。范文包含现象、机制和具体行动，写作主题不得同时重复阅读的主领域与冲突机制。

## 4. 权威记录、审查与校准

每道客观题只有一份权威记录：题号、四项、答案字母、准确答案文本、主类型/构念、次操作、证据跨度、最强干扰项和唯一失败边界。答案和解析均由它派生。换序后重新生成字母并复查。

按 WORKFLOW 的状态机推进。盲解者不看答案，记录首选、第二选择、信心、证据和争议；反方审查尽力证明第二答案。高置信分歧、无语法答案或两项可辩护必须退回。机器结构通过不能替代语义、来源和视觉审查。

难度只写 `predicted_easy/medium/hard`，有首次限时数据后再分析。低正确率但高低组差小优先视为噪声/坏题；集中高置信错误优先复核答案；耗时长且区分弱优先怀疑负担或歧义。少于约20名可比考生只作诊断。

## 5. 格式规范

A4纵向为默认；正文10—10.5pt，题目9.5—10pt，选项至少9pt，来源注至少8pt，图表文字至少8.5pt；边距通常不小于16mm，行距约1.15—1.3。英文正文首行缩进约两字符，方向、题目、选项、标题和表格不缩进。完形选项无框对齐。干净卷不含答案、来源、难度、学习任务或审查记录。

解析按“答案—证据—释义/推理—最强干扰项边界—其余选项”组织，避免整页文字墙。最终两份文件逐页渲染检查；任何字体、分页、图像或坐标变化后重查。记录页数、哈希和真实视觉证据。
