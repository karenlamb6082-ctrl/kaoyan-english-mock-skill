# 平台无关生产流程 1.1

## 交付阶段

| 阶段 | 可以说明什么 | 不可以说明什么 |
|---|---|---|
| DRAFT | 内容已经生成 | 已通过检查 |
| STRUCTURE_CHECKED | 结构和机器规则通过 | 答案唯一、来源真实 |
| SEMANTICALLY_REVIEWED | 独立/新鲜上下文完成答题与反方审查 | 来源和PDF已检查 |
| SOURCE_VERIFIED | 主张级来源与改写边界完成核验 | PDF视觉已通过 |
| VISUALLY_REVIEWED | 最终版本每页已检查 | 已有考生难度证据 |
| PILOT_READY | 可交给考生首次限时试做 | 真题统计等值 |
| EMPIRICALLY_REVIEWED | 实测数据和处理决定已记录 | 大样本心理测量等值，除非另有充分证据 |

## 职责隔离

`setter` 写题；`blind_solver` 不看答案独立作答；`adversarial_reviewer` 尝试证明第二答案；`source_reviewer` 核查来源；`visual_reviewer` 逐页检查；`release_owner` 裁决并推进阶段。同一对话换角色只能标记 `not_independent`；同一模型的隔离新会话、且未收到答案或此前解答时可记 `fresh_context`。看过答案的上下文不能再充当盲审者。

关卡通过须记录 `reviewed_revision`；语义关卡须明确 `key_visible: false`。检查器拦截缺字段、版本不符与跳级声明，但不能识别伪造证据；发布负责人必须实际查看记录。示例和含迁移占位内容的稿件不能进入发布阶段。

## 每轮流程

1. 报告实际能力，加载用户选择的状态与可核验近期真题基线。
2. 做双向钢人门审，明确替代目标、反方边界和决定性证据。
3. 先设计整卷生态，再选择具体来源和写文章。
4. 建立文章级来源卡和段落/主张级来源映射。
5. 生成一个权威客观题表；答案、解析和渲染均由它派生。
6. 运行结构检查，再做盲解、反方、来源和视觉审查。
7. 内容变化使受影响项目退回 `STRUCTURE_CHECKED`；排版变化使最终文件退回 `SOURCE_VERIFIED`。
8. 冻结 revision，分离干净卷和解析。文件名中的“最终版”不能覆盖门禁状态。
9. 收集首次限时数据，先裁决争议，再更新难度、质量债和下一套状态。

## 状态与轮换

永久规则、当前状态、历史档案分开。Part B 和写作按“最近最少使用”轮换，不永久排除形式；复用形式时改变线索结构和主题机制。缺历史就建立新系列或请求状态，不能编造已完成记录。

## 无法执行时

没有联网：使用用户提供的可核验原文或停在蓝图。没有代码：人工检查但不声称脚本通过。没有独立审查：最多停在 `STRUCTURE_CHECKED`。没有图像/PDF能力：提供制作说明或 HTML 候选稿，但不声称视觉完成。
