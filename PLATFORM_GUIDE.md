# 平台适配：先看能力，再看品牌

本文件不是任何产品当前界面的安装教程；未在所有平台实机测试，不保证每个账号、模型或套餐具备相同功能。
ChatGPT、Claude、千问、Gemini、DeepSeek等名称仅表示可能的使用环境，执行前逐项确认实际能力。

| 实際模式 | 启动材料 | 可交付范围 |
|---|---|---|
| 纯文字聊天 | UNIVERSAL_PROMPT.md分段粘贴＋状态 | DRAFT；来源由用户提供时可做内容审查 |
| 支持文件阅读 | 上传单文件规则＋状态，写JSON前加PAPER_SCHEMA | DRAFT；结构字段可人工核对 |
| 支持代码与文件 | 完整解压包＋schema＋本地检查器 | 最多STRUCTURE_CHECKED，另需独立语义和视觉门 |
| 支持浏览和图像 | 在上述基础上核验具体题源、制作并查看视觉材料 | 可继续推进SOURCE_VERIFIED和VISUALLY_REVIEWED |
| 原生技能目录 | 使用根SKILL.md，保留全部相对路径 | 不修改核心的工具增强入口 |

## 能力报告模板

## 直接交付PDF的必要能力

需要的是“模型加当前平台工具”的文件生成能力，而非某个模型名称。至少应实际验证：

1. 创建包含中文、英文及图形的一页PDF。
2. 返回可下载的真实文件，由用户成功打开。
3. 为正式整卷制作作文视觉材料。
4. 将最终PDF每页渲染为图片并实际查看，检查乱码、裁切和分页。

前两项缺失，不能承诺直接交付PDF；后两项缺失，要保留图像/视觉审核缺口。单页测试不证明长文档排版稳定，也不代替独立盲审。

代码执行环境若有合适的PDF库和文件下载能力，也可能满足要求；不要求模型“原生输出PDF”。HTML只能算中间稿，必须实际导出文件才能称为PDF已生成。无论工具多完整，发布状态仍须按WORKFLOW的关卡顺序推进。

## 能力报告填写
联网检索：可用/不可用/未知，证据：
附件读取：可用/不可用/未知，证据：
代码执行与下载：可用/不可用/未知，证据：
图片生成与查看：分别报告：
PDF导出与逐页渲染：分别报告：
独立盲审：当前是否真实可执行：
本次发布阶段和未完成门：

所有平台共用CORE_SPEC，适配时只改变工具调用与文件交接方式，不删答案唯一性、题源和难度边界。
原生技能可选agents/openai.yaml只提供UI说明，不是其他平台的依赖。
